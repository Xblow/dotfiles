snippet for "for loop assuming int variable" b
for (int ${1:i}=${3:0}; $1<${2:count}; ++$1)$0
endsnippet

# ${VISUAL}$0
# }

snippet de "Print variable/array for debugging" b
if (DEBUG) cout << "`!p import re;snip.rv=re.sub(r'\[([^\[]+)\]', r'["<<\1<<"]',t[1])`: " << ${1:variable} << endl;$0
endsnippet

snippet out "Print variable/array for debugging" b
std::cout << "`!p import re;snip.rv=re.sub(r'\[([^\[]+)\]', r'["<<\1<<"]',t[1])`: " << ${1:variable} << std::endl;$0
endsnippet
